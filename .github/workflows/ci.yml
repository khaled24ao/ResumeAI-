name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        # تم التعديل للاختبار على فولدر backend فقط لرفع نسبة الـ Coverage الحقيقية
        run: pytest --cov=backend --cov-report=xml --cov-fail-under=50
      - name: Upload coverage
        uses: codecov/codecov-action@v3

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker image
        run: docker build -t app .
      - name: Test Docker image
        run: docker run -d -p 7860:7860 app
